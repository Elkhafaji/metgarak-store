import {prisma} from '@/lib/prisma'; export default async function Addresses(){const user=await prisma.user.findFirst({include:{addresses:true}});return <div className="orders"><h1>العناوين</h1><button className="btn">إضافة عنوان جديد</button>{user?.addresses.map(a=><div className="order" key={a.id}><div><h3>{a.label}</h3><p>{a.street} — {a.city}</p><p className="muted">{a.phone}</p></div><div><button className="btn secondary">تعديل</button><button className="btn secondary">حذف</button></div></div>)}</div>}
