'use client'; import {useState} from 'react'; import StoreShell from '@/components/StoreShell'; export default function Register(){const [msg,setMsg]=useState(''); async function submit(e:any){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();setMsg(j.message||j.error)} return <StoreShell><div className="container"><form className="form" style={{maxWidth:600,margin:'50px auto'}} onSubmit={submit}><h1>إنشاء حساب جديد</h1><input className="input" name="name" placeholder="الاسم الكامل" required/><input className="input" name="email" type="email" placeholder="البريد الإلكتروني" required/><input className="input" name="phone" placeholder="رقم الهاتف"/><input className="input" name="password" type="password" placeholder="كلمة المرور" required/><button className="btn">إنشاء الحساب</button>{msg&&<p className="notice">{msg}</p>}</form></div></StoreShell>}
