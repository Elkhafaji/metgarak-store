import {prisma} from '@/lib/prisma'; import StoreShell from '@/components/StoreShell'; import ProductCard from '@/components/ProductCard';
export default async function Offers(){const ps=await prisma.product.findMany({where:{oldPrice:{not:null}},orderBy:{price:'asc'}});return <StoreShell><div className="container"><section className="hero"><h1>أفضل العروض و<span>التخفيضات</span></h1><p>منتجات مختارة بأسعار أقل — لا تفوّت الفرصة.</p><a className="btn" href="#deals">تسوق الآن</a></section><div id="deals" className="grid">{ps.map(p=><ProductCard key={p.id} p={p}/>)}</div></div></StoreShell>}
