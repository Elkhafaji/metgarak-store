import Link from 'next/link';
export default function ProductCard({p}:{p:any}){return <article className="card"><div style={{display:'flex',justifyContent:'space-between'}}><span className="badge">{p.badge||'جديد'}</span><span>♡</span></div><div className="product-img">🖥️</div><h3>{p.name}</h3><p className="muted">{p.description}</p><div>⭐ {p.rating} <span className="muted">({p.reviews})</span></div><p><span className="price">{p.price.toLocaleString('ar-EG')} ج.م</span>{p.oldPrice&&<span className="old">{p.oldPrice.toLocaleString('ar-EG')}</span>}</p><Link className="btn" href={`/products/${p.slug}`}>أضف إلى السلة 🛒</Link></article>}
