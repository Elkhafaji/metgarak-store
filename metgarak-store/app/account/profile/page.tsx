import {prisma} from '@/lib/prisma'; export default async function Profile(){const u=await prisma.user.findFirst();return <div className="profile"><h1>معلومات الحساب</h1><input className="input" defaultValue={u?.name} placeholder="الاسم الكامل"/><input className="input" defaultValue={u?.email} placeholder="البريد الإلكتروني"/><input className="input" defaultValue={u?.phone||''} placeholder="رقم الهاتف"/><button className="btn">حفظ التغييرات</button></div>}
