import {prisma} from '@/lib/prisma'; import ProductCard from '@/components/ProductCard'; export default async function Wishlist(){const ws=await prisma.wishlist.findMany({include:{product:true}});return <div className="orders"><h1>قائمة الأمنيات</h1><p className="muted">المنتجات التي حفظتها للرجوع إليها لاحقاً.</p><div className="grid">{ws.map(w=><ProductCard p={w.product} key={w.id}/>)}</div></div>}
