services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: metgark
      POSTGRES_PASSWORD: metgark
      POSTGRES_DB: metgark
    ports:
      - "5432:5432"
    volumes:
      - metgark_pg:/var/lib/postgresql/data
volumes:
  metgark_pg:
